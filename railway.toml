[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["mcp-server/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
startCommand = "node dist/index.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
