# Substrate MCP Server Configuration

# Transport mode: 'stdio' for local Claude Desktop, 'sse' for remote HTTP access
SUBSTRATE_TRANSPORT=sse

# Server port (for SSE mode)
PORT=3000

# API key for authentication (REQUIRED for production)
# Generate with: openssl rand -hex 32
SUBSTRATE_API_KEY=

# CORS origins (comma-separated, default: *)
SUBSTRATE_CORS_ORIGINS=*

# Data directory for SQLite and JSONL storage
SUBSTRATE_DATA_DIR=/data

# Qdrant configuration
SUBSTRATE_QDRANT_URL=http://localhost:6333
SUBSTRATE_QDRANT_COLLECTION=substrate_observations
# SUBSTRATE_QDRANT_API_KEY=  # If using Qdrant Cloud

# Logging level: debug, info, warn, error
SUBSTRATE_LOG_LEVEL=info

# Agent identifier (optional, for tracking)
SUBSTRATE_AGENT_ID=substrate-server
